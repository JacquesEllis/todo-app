rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder — Data Engineer will replace this
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
