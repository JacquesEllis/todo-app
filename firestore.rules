rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Deny everything by default — no read or write permitted to any path for any user
    match /{document=**} {
      allow read, write: if false;
    }

  }
}